<div class="text-center">
    <h2>{{title}}</h2>
    <p>{{post_body}}</p>
    <p>Written by {{user.name}} on {{format_date date_created}}</p>
    <button class="btn btn-primary" id="comment">Got a comment?</button>
</div>